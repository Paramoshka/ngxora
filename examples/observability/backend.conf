server {
    listen 80;
    server_name _;

    location / {
        default_type application/json;
        return 200 '{"service":"backend","status":"ok"}\n';
        add_header X-Backend demo;
    }
}
